<%= form_for(@welcome) do |f| %>
  <% if @welcome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@welcome.errors.count, "error") %> prohibited this welcome from being saved:</h2>

      <ul>
      <% @welcome.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :columnone %><br>
    <%= f.text_area :columnone %>
  </div>
  <div class="field">
    <%= f.label :columntwo %><br>
    <%= f.number_field :columntwo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
